# Parent disposition of first native review

All three P1 findings valid; parent fixed them after writer and reviewer finished (one source writer). Source remains uncommitted on main at4bbd8c2. Current complete patch: final.diff in this directory. No install/gameplay/provider calls.

1. BridgeSession.Release now latches _cleanupFailed at the throw site, not just Fail's catch. Fail preserves mutation_cleanup_failed if the latch is already set. Existing successful-completion cleanup fixture extended to assert failed Retire and repeated fresh-boundary refusal. Genuine RED against worker's compiled candidate: cleanup-red.log/exit134. After just this fix, next test reached and exposed P1#2 (event-red.log/exit134).
2. EventEntry.Close records CleanupFailed before rethrowing and still closes/clears its callback exactly once. Thus Fail's intentional swallowing can no longer erase cleanup outcome. BridgeSession.Retire(Action? cleanup) runs idle-entry close/check inside the same Release callback chain as other owners, with failure isolation. BeginRunEpoch captures the old EventEntry; its retirement callback also rejects historical CleanupFailed when Close is already a no-op. No event cleanup exception escapes this new boundary. Added idle-entry-fails-at-boundary and entry-failed-through-Fail-before-boundary fixtures, repeated boundaries, exact-once counts. A mutation removing ONLY historical CleanupFailed recording passes the idle case but fails prior failure=True, as intended (mutation-event-history/native-check.log,134). Restored.
3. DispatchMap captures the original queue and executor at registration; VoteFinished and retirement unsubscribe from that queue. DispatchTreasureOpen similarly captures original queue/executor. Publisher unsubscriptions are individual existing OnRelease callbacks, so no new framework/helper needed. Managed tests invoke those actual production compiler-generated unsubscribe closures with real native publisher objects constructed WITHOUT constructors, no game actions or Godot. A synthetic native RunManager's publisher property is replaced before BeginRunEpoch; tests assert old publisher loses its handler, replacement keeps its handler, and later boundaries don't unsubscribe again. Mutation reverting only map retirement unsubscribe to manager.ActionQueueSet builds but fails 'DispatchMap retirement removes only the original publisher\'s handler' (mutation-publisher/native-check.log,134). Restored.

Parent review-fixes gates: native build0 with0 warnings, native2692 checks0, app51 tests196 assertions0, lint/format/typecheck0; commands.jsonl and full logs under review-fixes/. A final restored-source run is next/under final/. Original worker RED is wiring-only (missing fresh-run hook); do not call it a full live or behavioral lifecycle reproduction. New parent RED/mutations are genuine causal cleanup/publisher regressions.

Please recheck the three findings, any new regression in their blast radius, and whether prior selector/new-run proof still stands. Do not edit source, run gameplay or install. Previous worker recommendation implied installation under prior authority; that is incorrect: this new candidate has NO installation or live-run approval yet. Parent will ask owner after final review/gates.
